# StreetSpeak AI starts in mock mode and should not require secrets.
STREETSPEAK_MODE=mock
LIVE_TRADING_ENABLED=false

# Future providers should use encrypted OS/keychain storage, not plaintext env files.
# Add placeholder names here only when the app actually reads them.
# BROKER_API_KEY=
# VOICE_PROVIDER_API_KEY=
